const createContent = require("./tweetsToCSV").createContent;

// TODO: mock originalTweetsNoMentions object & rewrite code so methods can be
// tested before all data is processed
test("createContent concatenates input to csv formatted text fields", () => {
  expect(true).toBeTrue();
});
